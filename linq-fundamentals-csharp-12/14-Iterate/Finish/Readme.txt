To run this project in Visual Studio Code, open the LINQSamples.code-workspace
  When prompted to add "Required Assets", Click the 'Yes' button
  Run the project
  Check the DEBUG CONSOLE window for the output
To run this project in Visual Studio 2022, open the LINQSamples.sln
  Run the project
  A console window shows the output


SampleViewModel Samples
--------------------------------------------------
ForEach() - ForEach allows you to iterate over a collection to perform assignments within each object.
ForEachSubQuery() - Iterate over each object in the collection and call a method to set a property
ForEachCallingMethod() - Iterate over each object in the collection and call a method to set a property
